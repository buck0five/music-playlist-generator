// config/icecast.js

module.exports = {
  remoteUser: '',  // Your Icecast server username
  remoteHost: '',  // Your Icecast server IP or hostname
  remotePath: '',  // The path on the Icecast server where the playlist should be uploaded
};
